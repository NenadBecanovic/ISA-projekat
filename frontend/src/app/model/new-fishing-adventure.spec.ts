import { NewFishingAdventure } from './new-fishing-adventure';

describe('NewFishingAdventure', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new NewFishingAdventure()).toBeTruthy();
  });
});
