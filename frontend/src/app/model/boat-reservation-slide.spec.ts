import { BoatReservationSlide } from './boat-reservation-slide';

describe('BoatReservationSlide', () => {
  it('should create an instance', () => {
    expect(new BoatReservationSlide()).toBeTruthy();
  });
});
