import { HouseReservationSlide } from './house-reservation-slide';

describe('HouseReservationSlide', () => {
  it('should create an instance', () => {
    expect(new HouseReservationSlide()).toBeTruthy();
  });
});
