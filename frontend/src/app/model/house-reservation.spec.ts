import { HouseReservation } from './house-reservation';

describe('HouseReservation', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new HouseReservation()).toBeTruthy();
  });
});
